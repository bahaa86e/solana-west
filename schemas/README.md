# Schemas moved

JSON-LD builders live under **`lib/schema/jsonld/`**.

Import from:

`@/lib/schema/jsonld/<page-module>`

Example: `@/lib/schema/jsonld/blog-article-page`.

This folder remains as a pointer for older bookmarks and docs.
