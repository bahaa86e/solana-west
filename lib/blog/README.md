# Blog utilities (reserved)

Use this folder for shared blog helpers (e.g. excerpt builders, reading-time, cluster helpers) as the article count grows.

Current blog logic remains in `data/blog/` (types, catalog, article modules, governance).
